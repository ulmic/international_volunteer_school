- model_class = Page
- title model_class.model_name.human.pluralize(:ru), :admin
.page-header
  %h1
    =t model_class.model_name.human.pluralize(:ru)
    = link_to "#{t 'helpers.links.new'}", new_admin_page_path, class: 'btn btn-primary'
  = render 'pages_list', pages: @pages
  = link_to "#{t 'helpers.links.new'}", new_admin_page_path, class: 'btn btn-primary'
